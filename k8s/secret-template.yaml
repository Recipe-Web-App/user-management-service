---
# k8s/secret-template.yaml
apiVersion: v1
kind: Secret
metadata:
  name: user-management-db-password
  namespace: user-management
type: Opaque
stringData:
  USER_MANAGEMENT_DB_PASSWORD: "${USER_MANAGEMENT_DB_PASSWORD}" # Injected during deployment

