apiVersion: v1
kind: Service
metadata:
  name: user-management-service
  namespace: user-management-dev-poc
  labels:
    app: user-management-service
spec:
  type: ClusterIP
  selector:
    app: user-management-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
