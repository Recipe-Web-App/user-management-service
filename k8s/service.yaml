---
# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: user-management
  namespace: user-management
spec:
  selector:
    app: user-management
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
